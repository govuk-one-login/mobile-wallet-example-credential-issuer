server:
  requestLog:
    appenders:
      - type: console
        layout:
          type: access-json
        filterFactories:
          - type: uri
            uris:
              - "/healthcheck"
logging:
  level: INFO
  appenders:
    - type: console
      layout:
        type: json

httpClient:
  timeout: 5000ms
  connectionTimeout: 5000ms
  connectionRequestTimeout: 5000ms
